/* SCAFFOLD — owned by the UI agent. Replace with the real implementation. */
export class UISystem {
  constructor(game) { this.game = game; this.root = game.uiRoot; }
  async init() {}
  onRaceStart() {}
  onRaceEnd() {}
  showScreen() {}
  update() {}
  dispose() {}
}
export default UISystem;
