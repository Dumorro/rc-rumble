/* SCAFFOLD — owned by the FX agent. Replace with the real implementation. */
export class FXSystem {
  constructor(game) { this.game = game; }
  async init() {}
  onRaceStart() {}
  onRaceEnd() {}
  update() {}
  dispose() {}
}
export default FXSystem;
