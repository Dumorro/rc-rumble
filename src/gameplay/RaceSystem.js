/* SCAFFOLD — owned by the Gameplay agent. Replace with the real implementation. */
export class RaceSystem {
  constructor(game) { this.game = game; this.results = []; }
  async init() {}
  onRaceStart() {}
  onRaceEnd() {}
  fixedUpdate() {}
  dispose() {}
}
export default RaceSystem;
